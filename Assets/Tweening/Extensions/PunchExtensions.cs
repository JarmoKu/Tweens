using UnityEngine;

namespace JK.Tweening
{
    /*public static class PunchExtensions
    {
        public static PunchPositionTween PunchPosition (
            this Transform transform, Vector3 targetScale, float duration, Space space = Space.World)
        {
            return new PunchPositionTween (transform, targetScale, duration, space);
        }

        public static PunchRotationTween PunchRotation (
            this Transform transform, Vector3 targetScale, float duration, Space space = Space.World)
        {
            return new PunchRotationTween (transform, targetScale, duration, space);
        }

        public static PunchScaleTween PunchScale (
            this Transform transform, Vector3 targetScale, float duration)
        {
            return new PunchScaleTween (transform, targetScale, duration);
        }
    }*/
}