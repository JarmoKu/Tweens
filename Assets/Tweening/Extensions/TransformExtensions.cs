using System;
using UnityEngine;

public static class TransformExtensions
{
    /*public static void SetPosition (this Transform transform, Vector3 position, Space space)
    {
        if (space.HasFlag (Space.Self))
            transform.localPosition = position;
        else
            transform.position = position;
    }

    public static void SetRotation (this Transform transform, Vector3 rotation, Space space)
    {
        if (space.HasFlag (Space.Self))
            transform.localRotation = Quaternion.Euler (rotation);
        else
            transform.rotation = Quaternion.Euler (rotation);
    }

    public static Vector3 GetPosition (this Transform transform, Space space)
    {
        return space.HasFlag (Space.World) ? transform.position : transform.localPosition;
    }

    public static Vector3 GetRotation (this Transform transform, Space space)
    {
        return space.HasFlag (Space.World) ? transform.eulerAngles : transform.localEulerAngles;
    }

    public static bool EqualsSpace (this Space value1, Space value2)
    {
        return (int)value1 == (int)value2;
    }

    public static bool EqualsPlayOn (this PlayOn value1, PlayOn value2)
    {
        return (int)value1 == (int)value2;
    }

    public static bool EqualsTweenType (this TweenType value1, TweenType value2)
    {
        return (int)value1 == (int)value2;
    }

    public static bool EqualsTweenClass (this TweenClass value1, TweenClass value2)
    {
        return (int)value1 == (int)value2;
    }*/
}